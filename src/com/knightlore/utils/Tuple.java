package com.knightlore.utils;


// An arbitrary pair of objects.
public class Tuple<X, Y> {
    public X x; 
    public Y y; 
    public Tuple(X x, Y y) { 
      this.x = x; 
      this.y = y; 
    } 
}
